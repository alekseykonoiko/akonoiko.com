# FastHTML Application Environment Variables

# Secret key for session signing (REQUIRED in production)
# Generate a secure random key: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=change-me-in-production-use-secure-random-key

# Optional: Database connection string (if using database)
# DATABASE_URL=sqlite:///data/app.db

# Optional: Domain for Caddy (if different from default)
# DOMAIN=akonoiko.com
